.user{:id => "tweet_user-#{user.id}-#{name}"}
  = link_to image_tag(user.photo), user_url(user.screen_name)
  .body
    %span.who= link_to h(user.screen_name), user_url(user.screen_name)
    %span.when= t('user.created_at', :time => time_ago_in_words(user.created_at))
  .clear
  .info{:id => "info_user-#{user.id}-#{name}"}
    = t('global.categories')
    = user.lists.map { |list| link_to list.name }.join(', ')